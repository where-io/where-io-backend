<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!--
        Padrão (sem perfil ativo, Railway, produção): JSON estruturado em INFO.
        Ativa apenas com SPRING_PROFILES_ACTIVE=dev o appender legível com DEBUG.
    -->

    <springProfile name="!dev">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeCallerData>false</includeCallerData>
                <customFields>{"app":"where-io"}</customFields>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.mongodb.driver"  level="WARN"/>
    </springProfile>

    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} [reqId=%X{requestId}] - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.mongodb.driver"  level="WARN"/>
        <logger name="analu.whereio" level="DEBUG"/>
    </springProfile>

</configuration>
