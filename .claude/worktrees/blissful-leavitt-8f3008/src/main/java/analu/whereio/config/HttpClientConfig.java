package analu.whereio.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class HttpClientConfig {
    // RestTemplate bean is already configured in RestTemplateConfig
    // Add other HTTP client configurations here if needed
}
